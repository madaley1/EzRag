# Extra deps for running a local HuggingFace model (e.g. Qwen3-32B).
# Install in addition to requirements.txt:
#   pip install -r requirements-local-llm.txt
#
# ~104 GB RAM/VRAM required for Qwen3-32B bf16.
# On Apple Silicon set LOCAL_MODEL_DEVICE=mps (or leave as auto).
# On NVIDIA GPU set LOCAL_MODEL_DEVICE=auto and ensure CUDA is available.

transformers>=4.51.0
torch>=2.2.0
accelerate>=0.30.0
