# Test dependencies — not needed in production or Docker.
# pip install -r requirements-dev.txt
pytest>=8.0.0
pytest-mock>=3.14.0
httpx>=0.23.0,<0.28.0  # starlette 0.27 TestClient passes app= to httpx.Client; removed in 0.28
